class Solution:
    def uniqueOccurrences(self, arr: List[int]) -> bool:
        d = collections.Counter(arr)
        
            
            
               
            

        res = d.values()

        return len(res) == len(set(res))
        
